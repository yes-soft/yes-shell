/**插件配置文档,此文档自动生成请勿修改**/
(function (application) {
    var $code_template = true;
    var settings = application.plugin || {};

    if (!settings.pluginDefaultName)
        settings.pluginDefaultName = "$default";
}());